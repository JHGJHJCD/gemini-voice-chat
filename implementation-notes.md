# implementation-notes — v1.9 (25/9/2026)

## התוכנית
1. **מודלים חדשים:** ברירת מחדל `gemini-3.8-live` (GA), אפשרות `gemini-3.8-live-extended-thinking`,
   ו-`gemini-2.5-flash-native-audio-preview-12-2025` כ"ישן". בחירה בהגדרות. חשיבה: 3.8 בסיסי — בלי
   `thinking_config`; extended — `thinking_level` (low/medium/high); 2.5 — `thinking_budget`.
   נפילה חיננית: מודל לא נמצא → ניסיון עם המודל הבא בשרשרת; כישלון כלי (חיפוש) → ניסיון בלי כלים.
2. **שיחה בלי מגבלת 15 דק':** `context_window_compression` (חלון הזזה) + `session_resumption`
   (שימוש ב-handle בהתחברות מחדש) + טיפול ב-`go_away`.
3. **ביטול הד אמיתי (`aec.py`):** מסנן אדפטיבי בתחום התדר (PBFDAF) שמשתמש במה שבאמת הושמע ברמקול
   כאות ייחוס, מוריד את קול ג'מיני מהמיקרופון, ומעליו שער החלטה: "הד בלבד" → לא שולחים;
   "המשתמש מדבר" (שארית גדולה ביחס להד המשוער) → שולחים את האות המנוקה (התפרצות עובדת).
   לפני שהמסנן התכנס — נופלים לחוק הישן (סף עוצמה), עם סף שמותאם לעוצמת ההשמעה.
4. השלמת שחרור: גרסה 1.9, בנייה, חתימה, מתקין, release, commit.

## סטיות
- **גרסה 1.9 במקום "להשלים 1.8":** v1.8 מעולם לא שוחררה, ואת ה-exe שלה צריך היה לבנות מחדש
  בכל מקרה. שחרור אחד עם כל התוכן (אייקון, עדכון-מהתוכנה, התקנה בעברית + מודלים + ביטול הד) ברור
  יותר למשתמשים מאשר שניים.
- **בלי scipy:** התכנון היה להשתמש ב-`scipy.signal.resample_poly` להמרת 24k→16k. scipy היה מוסיף
  ~80MB למתקין. נכתב resampler פוליפאזי קטן ב-numpy (`aec._Resampler`) עם המשכיות בין בלוקים.
- **גלאי דיבור-כפול לפי מתאם (ρ):** הגישה הראשונה (סף לפי ERLE בלבד) נכשלה בבדיקה: באוזניות
  (אין הד) ובדיבור בו-זמני המדד ERLE "מתלכלך" והמשתמש נחסם. נוסף מתאם בין המיקרופון להד המשוער —
  הד נראה כמו הד (ρ גבוה), דיבור לא. בנוסף דהייה איטית של מקדמים בזמן דיבור-כפול.
- **בלוק הרמקול שונה מ-1600 ל-2400 דגימות (100ms ב-24kHz):** כדי שההמרה ל-16kHz תיתן בדיוק
  1600 דגימות לבלוק ואות הייחוס לא "יזחל" מול המיקרופון.
- **חיפוש Google ב-3.8:** התיעוד לא מאשר תמיכה. במקום להסיר — ניסיון עם כלים, ואם המודל דוחה,
  ניסיון חוזר אוטומטי בלי כלים עם הודעה קטנה בתמלול.
- **מפתח Picovoice בהגדרות נראה כמו מפתח Google** (`AIza...`) — לא נגעתי; מילת ההפעלה תיכשל
  אם היא מופעלת. לציין ליהודה.
- **אימות חי מול Gemini לא בוצע על-ידי (Claude):** כניסה עם המפתח של יהודה — לפי הכלל בפרויקט.
  נבדק: בדיקות יחידה (38), קומפילציה, בנייה, והרצת האפליקציה.
